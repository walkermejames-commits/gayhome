import { execFileSync } from "node:child_process";import fs from "node:fs/promises";
const branch=execFileSync("git",["branch","--show-current"],{encoding:"utf8"}).trim(),commit=execFileSync("git",["rev-parse","HEAD"],{encoding:"utf8"}).trim();const flags=JSON.parse(await fs.readFile("docs/phase-5/PILOT_FEATURE_FLAG_MATRIX.json","utf8"));const unsafe=flags.flags.filter(flag=>flag.default!==false);const report={mode:"dry-run",branch,commit,unsafeFlagDefaults:unsafe.map(flag=>flag.key),productionDeploymentSupported:false,deployed:false,checks:["migration record","dataset revision","release gates","backup reference","rollback version","smoke and emergency routes","authentication and access","safe exit and discreet mode","monitoring and alerts"]};console.log(JSON.stringify(report,null,2));if(unsafe.length)process.exit(1);
