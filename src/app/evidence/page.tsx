import{getCatalog}from"@/server/catalog";import{EvidenceList}from"@/components/evidence-list";export default async function Page(){const c=await getCatalog();return <><h1>Evidence</h1><p className="lede">See what may help, mark progress and prepare a safe index. Evidence is never attached automatically.</p><p className="callout">Files are not uploaded or stored. Status marks are session-only and disappear when this page closes.</p><EvidenceList items={c.evidenceChecklist.map(x=>({id:x.evidence_id,area:x.area,description:x.useful_evidence,safety:x.safety_note}))}/></>}
