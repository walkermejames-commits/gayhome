import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace title="Consent centre" intro="Consent is specific to a recipient, purpose, case, selected information, actions and time period."><div className="review-panel"><h2>Safe defaults</h2><p>Onward sharing and new information are off by default. Revocation stops linked future access immediately and remains in the audit history.</p></div><p>Open a case’s Sharing section to review its active, expired and revoked records.</p></SecureWorkspace>}
