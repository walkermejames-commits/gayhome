import Link from "next/link";import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace title="Your data" intro="Export before deletion and understand any lawful retention exception."><div className="button-row"><Link className="button-link secondary" href="/api/v1/account/export">Export account data</Link><Link className="button-link secondary" href="/case">Export a case</Link></div><p>Account deletion uses a 30-day request window. Audit and safeguarding records are retained only where an approved lawful basis requires it.</p></SecureWorkspace>}
