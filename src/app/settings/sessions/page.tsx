import Link from "next/link";import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace title="Sessions and devices" intro="Sign out this device or revoke every active session from the authenticated session control."><p><Link href="/profile">Open account and session controls</Link></p><p>Public-device mode never enables persistent case or evidence storage.</p></SecureWorkspace>}
