import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace title="Notification settings" intro="Choose neutral wording, categories, channels and quiet hours."><p className="callout">No external notification provider is connected. Preferences can be stored after deployment configuration, but messages are not sent.</p><p>Safe example: “You have an important task due tomorrow.”</p></SecureWorkspace>}
