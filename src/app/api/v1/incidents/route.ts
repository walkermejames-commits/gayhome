import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { incidentSchema } from "@/domain/pilot-operations";import { parseJson } from "@/server/case-api";import { requireOperationalActor } from "@/server/operations-auth";import { createIncident,listIncidents } from "@/server/pilot-operations";
export async function GET(request:NextRequest){const actor=await requireOperationalActor(request,["incident_commander","safeguarding_lead","pilot_administrator"]);if(actor instanceof Response)return actor;return NextResponse.json({incidents:await listIncidents()});}
export async function POST(request:NextRequest){const actor=await requireOperationalActor(request,["incident_commander","safeguarding_lead","pilot_administrator"],true);if(actor instanceof Response)return actor;const input=await parseJson(request,incidentSchema);if(input instanceof Response)return input;return NextResponse.json(await createIncident(actor.userId,input),{status:201});}
