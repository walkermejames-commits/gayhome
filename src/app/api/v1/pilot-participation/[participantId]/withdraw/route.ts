import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { requireParticipantOwner } from "@/server/participant-auth";import { withdrawParticipant } from "@/server/pilot-operations";export async function POST(request:NextRequest,{params}:{params:Promise<{participantId:string}>}){const{participantId}=await params;const actor=await requireParticipantOwner(request,participantId);if(actor instanceof Response)return actor;return NextResponse.json(await withdrawParticipant(actor.userId,participantId));}
