import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { z } from "zod";import { parseJson } from "@/server/case-api";import { requireParticipantOwner } from "@/server/participant-auth";import { requestParticipantData } from "@/server/pilot-operations";const schema=z.object({requestType:z.enum(["export","deletion"])}).strict();export async function POST(request:NextRequest,{params}:{params:Promise<{participantId:string}>}){const{participantId}=await params;const actor=await requireParticipantOwner(request,participantId);if(actor instanceof Response)return actor;const input=await parseJson(request,schema);if(input instanceof Response)return input;return NextResponse.json(await requestParticipantData(actor.userId,participantId,input.requestType));}
