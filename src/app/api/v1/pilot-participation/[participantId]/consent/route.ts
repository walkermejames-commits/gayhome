import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { participantConsentSchema } from "@/domain/pilot-operations";import { parseJson } from "@/server/case-api";import { requireParticipantOwner } from "@/server/participant-auth";import { setParticipantConsent } from "@/server/pilot-operations";
export async function POST(request:NextRequest,{params}:{params:Promise<{participantId:string}>}){const{participantId}=await params;const actor=await requireParticipantOwner(request,participantId);if(actor instanceof Response)return actor;const input=await parseJson(request,participantConsentSchema);if(input instanceof Response)return input;return NextResponse.json(await setParticipantConsent(actor.userId,participantId,input));}
