import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { notificationPreferenceSchema } from "@/domain/pilot-operations";import { parseJson } from "@/server/case-api";import { apiError } from "@/server/http";import { requireParticipantOwner } from "@/server/participant-auth";import { setNotificationPreference } from "@/server/pilot-operations";export async function POST(request:NextRequest,{params}:{params:Promise<{participantId:string}>}){const{participantId}=await params;const actor=await requireParticipantOwner(request,participantId);if(actor instanceof Response)return actor;const input=await parseJson(request,notificationPreferenceSchema);if(input instanceof Response)return input;try{return NextResponse.json(await setNotificationPreference(actor.userId,participantId,input));}catch(error){return apiError(409,"NOTIFICATION_DELIVERY_DISABLED",error instanceof Error?error.message:"Notification preference could not be saved.");}}
