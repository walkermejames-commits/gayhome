import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { releaseSchema } from "@/domain/pilot-operations";import { parseJson } from "@/server/case-api";import { requireOperationalActor } from "@/server/operations-auth";import { createRelease } from "@/server/pilot-operations";import { getPool } from "@/server/db/pool";
export async function GET(request:NextRequest){const actor=await requireOperationalActor(request,["release_manager","pilot_administrator"]);if(actor instanceof Response)return actor;return NextResponse.json({releases:(await getPool().query("SELECT * FROM operational_releases ORDER BY created_at DESC")).rows});}
export async function POST(request:NextRequest){const actor=await requireOperationalActor(request,["release_manager","pilot_administrator"],true);if(actor instanceof Response)return actor;const input=await parseJson(request,releaseSchema);if(input instanceof Response)return input;return NextResponse.json(await createRelease(actor.userId,input),{status:201});}
