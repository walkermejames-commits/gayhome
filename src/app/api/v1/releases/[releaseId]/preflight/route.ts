import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { apiError } from "@/server/http";import { requireOperationalActor } from "@/server/operations-auth";import { releasePreflight } from "@/server/pilot-operations";
export async function GET(request:NextRequest,{params}:{params:Promise<{releaseId:string}>}){const actor=await requireOperationalActor(request,["release_manager","pilot_administrator"]);if(actor instanceof Response)return actor;const result=await releasePreflight((await params).releaseId);return result?NextResponse.json(result):apiError(404,"RELEASE_NOT_FOUND","Release record not found.");}
