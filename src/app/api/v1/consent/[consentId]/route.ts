import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { revokeConsent } from "@/server/casework";import { requireMutation,unexpectedApiError } from "@/server/case-api";import { apiError,publicNoStoreHeaders } from "@/server/http";
export async function DELETE(request:NextRequest,{params}:{params:Promise<{consentId:string}>}){const auth=await requireMutation(request);if(auth instanceof NextResponse)return auth;try{return await revokeConsent(auth,(await params).consentId)?Response.json({data:{revoked:true,effective:"immediate"}},{headers:publicNoStoreHeaders}):apiError(404,"CONSENT_NOT_FOUND","The consent was not found or is already revoked.")}catch{return unexpectedApiError()}}
