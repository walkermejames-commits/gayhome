import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { partnerSchema } from "@/domain/pilot-operations";import { parseJson } from "@/server/case-api";import { requireOperationalActor } from "@/server/operations-auth";import { createPartner,listPartnersFor } from "@/server/pilot-operations";
export async function GET(request:NextRequest){const actor=await requireOperationalActor(request,["partner_administrator","data_reviewer","pilot_administrator"]);if(actor instanceof Response)return actor;return NextResponse.json({organisations:await listPartnersFor(actor)});}
export async function POST(request:NextRequest){const actor=await requireOperationalActor(request,["partner_administrator","pilot_administrator"],true);if(actor instanceof Response)return actor;const input=await parseJson(request,partnerSchema);if(input instanceof Response)return input;return NextResponse.json(await createPartner(actor.userId,input),{status:201});}
