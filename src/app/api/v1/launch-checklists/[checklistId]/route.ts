import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { apiError } from "@/server/http";import { requireOperationalActor } from "@/server/operations-auth";import { checklistStatus } from "@/server/pilot-operations";
export async function GET(request:NextRequest,{params}:{params:Promise<{checklistId:string}>}){const actor=await requireOperationalActor(request,["release_manager","pilot_administrator","commissioner_viewer"]);if(actor instanceof Response)return actor;const status=await checklistStatus((await params).checklistId);return status?NextResponse.json(status):apiError(404,"CHECKLIST_NOT_FOUND","Launch checklist not found.");}
