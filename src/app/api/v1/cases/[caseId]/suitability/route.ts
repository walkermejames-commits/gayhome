import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { suitabilitySchema } from "@/domain/casework";import { createSuitability } from "@/server/casework";import { parseJson,requireMutation,unexpectedApiError } from "@/server/case-api";import { apiError,publicNoStoreHeaders } from "@/server/http";
export async function POST(request:NextRequest,{params}:{params:Promise<{caseId:string}>}){const auth=await requireMutation(request);if(auth instanceof NextResponse)return auth;const input=await parseJson(request,suitabilitySchema);if(input instanceof NextResponse)return input;try{const data=await createSuitability(auth,(await params).caseId,input);return data?Response.json({data},{status:201,headers:publicNoStoreHeaders}):apiError(404,"CASE_NOT_FOUND","The case was not found or you cannot create a suitability assessment.")}catch{return unexpectedApiError()}}
