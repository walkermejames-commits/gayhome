import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { z } from "zod";import { parseJson } from "@/server/case-api";import { apiError } from "@/server/http";import { requireOperationalActor } from "@/server/operations-auth";import { linkPublishedCorrection } from "@/server/pilot-operations";
const schema=z.object({datasetRevisionId:z.uuid()}).strict();export async function POST(request:NextRequest,{params}:{params:Promise<{proposalId:string}>}){const actor=await requireOperationalActor(request,["data_reviewer","pilot_administrator"],true);if(actor instanceof Response)return actor;const input=await parseJson(request,schema);if(input instanceof Response)return input;try{return NextResponse.json(await linkPublishedCorrection(actor.userId,(await params).proposalId,input.datasetRevisionId));}catch(error){return apiError(409,"CORRECTION_PUBLICATION_BLOCKED",error instanceof Error?error.message:"Correction publication was blocked.");}}
