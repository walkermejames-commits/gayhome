import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { feedbackSchema } from "@/domain/pilot-operations";import { apiSession,caseRateLimit,parseJson } from "@/server/case-api";import { apiError,originIsTrusted } from "@/server/http";import { requireOperationalActor } from "@/server/operations-auth";import { createFeedback,listFeedback } from "@/server/pilot-operations";
export async function GET(request:NextRequest){const actor=await requireOperationalActor(request,["support_agent","safeguarding_lead","pilot_administrator","commissioner_viewer"]);if(actor instanceof Response)return actor;return NextResponse.json({feedback:await listFeedback()});}
export async function POST(request:NextRequest){if(!originIsTrusted(request))return apiError(403,"ORIGIN_REJECTED","The request origin is not trusted.");const session=await apiSession(request);const key=session?.userId??request.headers.get("x-forwarded-for")?.split(",")[0]?.trim()??"anonymous";if(!await caseRateLimit(key,"feedback",15))return apiError(429,"RATE_LIMITED","Too many feedback requests. Please try again later.");const input=await parseJson(request,feedbackSchema);if(input instanceof Response)return input;if(!session&&!input.anonymous)return apiError(401,"AUTH_REQUIRED","Sign in or submit anonymously.");return NextResponse.json(await createFeedback(session?.userId??null,input),{status:201});}
