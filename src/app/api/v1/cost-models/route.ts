import type { NextRequest } from "next/server";import { NextResponse } from "next/server";import { costModelSchema } from "@/domain/pilot-operations";import { parseJson } from "@/server/case-api";import { requireOperationalActor } from "@/server/operations-auth";import { createCostModel } from "@/server/pilot-operations";import { getPool } from "@/server/db/pool";
export async function GET(request:NextRequest){const actor=await requireOperationalActor(request,["pilot_administrator","commissioner_viewer","release_manager"]);if(actor instanceof Response)return actor;return NextResponse.json({models:(await getPool().query("SELECT * FROM operating_cost_models ORDER BY created_at DESC")).rows});}
export async function POST(request:NextRequest){const actor=await requireOperationalActor(request,["pilot_administrator","release_manager"],true);if(actor instanceof Response)return actor;const input=await parseJson(request,costModelSchema);if(input instanceof Response)return input;return NextResponse.json(await createCostModel(actor.userId,input),{status:201});}
