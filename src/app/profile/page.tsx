import{ProfileForm}from"@/components/profile-form";const sections=[["contact-safety","Safe contact"],["housing","Housing situation"],["accessibility","Health and accessibility"]];export default function Page(){return <><h1>My profile</h1><p className="lede">Add only the details useful for your next step. You never have to complete the whole profile.</p><p className="status">Guest · not saved</p><div className="action-grid">{sections.map(([id,title])=><a className="action-card" key={id} href={`/profile/${id}`}><strong>{title}</strong><span>Review, update or remove details.</span></a>)}</div><ProfileForm/></>}
