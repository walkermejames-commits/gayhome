import{notFound}from"next/navigation";import{ProfileForm}from"@/components/profile-form";const names:Record<string,string>={"contact-safety":"Safe contact","housing":"Housing situation","accessibility":"Health and accessibility"};export default async function Page({params}:{params:Promise<{section:string}>}){const{section}=await params,title=names[section];if(!title)notFound();return <><h1>{title}</h1><p className="lede">Answer only what helps with your current task. Sensitive details always need explicit review.</p><ProfileForm section={title}/></>}
