import { Phase6AdminWorkspace } from "@/components/phase6-workspaces";export default function Page(){return <Phase6AdminWorkspace roles={["administrator","commissioner"]} title="Funder pack preview" intro="Non-sensitive project claims with explicit delivery status."><p>Every statement must be labelled implemented, in development, planned, tested, approved or not yet measured. The generator does not invent outcomes or inflate impact figures.</p></Phase6AdminWorkspace>}
