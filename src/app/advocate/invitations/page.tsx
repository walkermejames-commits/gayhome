import { AdvocateInviteForm } from "@/components/advocate-invite-form";import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace title="Advocate invitations" intro="Choose a person, permissions and an expiry. Sensitive categories are separate and excluded here by default."><p className="callout">External email is disabled while MAIL-001 is open. The app prepares an invitation for you to copy through a safe channel.</p><AdvocateInviteForm/></SecureWorkspace>}
