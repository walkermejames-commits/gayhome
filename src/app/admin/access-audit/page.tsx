import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace requiredRole="administrator" title="Access audit" intro="Review grants, revocations, suspicious access and break-glass events without casual content browsing."><p>Audit records contain identifiers and action metadata only—not document bodies, evidence, medical details or abuse narratives.</p></SecureWorkspace>}
