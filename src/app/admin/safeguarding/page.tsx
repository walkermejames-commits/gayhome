import { SecureWorkspace } from "@/components/secure-workspace";export default function Page(){return <SecureWorkspace requiredRole="safeguarding_reviewer" title="Safeguarding review queue" intro="Operational ownership and lawful basis must be approved before real escalation is enabled."><p className="callout">Real safeguarding referrals are disabled while SAFEGUARD-001 awaits human approval.</p></SecureWorkspace>}
