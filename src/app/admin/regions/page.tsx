import Link from "next/link";import { Phase6AdminWorkspace } from "@/components/phase6-workspaces";import { regionRegistry } from "@/phase6/regions";
export default function Page(){return <Phase6AdminWorkspace title="Regional datasets" intro="Review isolation, ownership and publication state without exposing case data."><ul className="service-list">{regionRegistry.map((region)=><li className="service-card" key={region.id}><h2><Link href={`/admin/regions/${region.id}`}>{region.name}</Link></h2><p>Status: {region.status}. Dataset: {region.datasetRevision??"not supplied"}.</p></li>)}</ul></Phase6AdminWorkspace>}
