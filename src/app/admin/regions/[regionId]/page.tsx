import { Phase6AdminWorkspace } from "@/components/phase6-workspaces";import { regionRegistry } from "@/phase6/regions";
export default async function Page({params}:{params:Promise<{regionId:string}>}){const {regionId}=await params;const region=regionRegistry.find((item)=>item.id===regionId);return <Phase6AdminWorkspace title={region?.name??"Unknown region"} intro="Regional governance and publication controls."><dl><dt>Public status</dt><dd>{region?.status??"not configured"}</dd><dt>Dataset</dt><dd>{region?.datasetRevision??"not supplied"}</dd><dt>Routing</dt><dd>{region?.status==="published"?"Existing controlled behavior":"Blocked"}</dd></dl></Phase6AdminWorkspace>}
