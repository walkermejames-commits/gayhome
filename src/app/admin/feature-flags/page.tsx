import { FeatureFlagForm } from "@/components/operational-forms";import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";import { featureFlagIds } from "@/domain/pilot-operations";
export default function Page(){return <SecureOperationalWorkspace roles={["pilot_administrator","incident_commander","release_manager"]} title="Feature flag register" intro="The canonical Phase 5 flag set is default-deny. Dependencies are checked server-side on every enable request."><div className="table-scroll"><table><caption>Phase 5 default state</caption><thead><tr><th scope="col">Flag</th><th scope="col">Default</th><th scope="col">Emergency action</th></tr></thead><tbody>{featureFlagIds.map(flag=><tr key={flag}><th scope="row">{flag}</th><td>Disabled</td><td>{flag==="production.public_access"?"Cannot be enabled here":"Can be disabled immediately"}</td></tr>)}</tbody></table></div><FeatureFlagForm/></SecureOperationalWorkspace>}
