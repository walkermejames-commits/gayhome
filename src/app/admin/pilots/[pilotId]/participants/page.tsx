import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{pilotId:string}>}){const{pilotId}=await params;return <SecureOperationalWorkspace roles={["pilot_administrator","support_agent","safeguarding_lead"]} title="Pilot participants" intro={`Manage invitations, accessibility preferences, separate consents and withdrawal for ${pilotId}.`}><p>Operational listings expose status and event dates only. Safe contact and support information remain encrypted.</p><ul><li>Withdrawal revokes every optional consent.</li><li>Data export and deletion requests are recorded separately.</li><li>Emergency public information never requires participation.</li></ul></SecureOperationalWorkspace>}
