import Link from "next/link";import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{pilotId:string}>}){const{pilotId}=await params;return <SecureOperationalWorkspace roles={["pilot_administrator","incident_commander","safeguarding_lead"]} title="Pilot incidents" intro={`Containment and recovery actions for ${pilotId}.`}><Link href="/admin/incidents">Open incident command</Link><p>Feature shutdown and pilot pause actions record the previous state, new state, reason and actor.</p></SecureOperationalWorkspace>}
