import { CohortForm } from "@/components/operational-forms";import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{pilotId:string}>}){const{pilotId}=await params;return <SecureOperationalWorkspace roles={["pilot_administrator"]} title="Pilot cohorts" intro={`Create and approve bounded cohorts for ${pilotId}. Cohort approval never enables its feature flags.`}><p className="callout">Cohort limits, consent version, permitted data and exit criteria are enforced by the cohort API. A participant cannot be invited into an unapproved cohort.</p><CohortForm pilotId={pilotId}/></SecureOperationalWorkspace>}
