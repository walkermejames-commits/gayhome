import Link from "next/link";import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{pilotId:string}>}){const{pilotId}=await params;return <SecureOperationalWorkspace roles={["pilot_administrator","support_agent","safeguarding_lead"]} title="Pilot support" intro={`Priority-safe support queue for ${pilotId}. Ticket listings omit encrypted descriptions.`}><Link href="/support-centre">Open support centre</Link><p>Safeguarding tickets generate a distinct escalation audit event and use the critical response queue when marked critical.</p></SecureOperationalWorkspace>}
