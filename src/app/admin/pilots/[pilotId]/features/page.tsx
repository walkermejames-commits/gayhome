import Link from "next/link";import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{pilotId:string}>}){const{pilotId}=await params;return <SecureOperationalWorkspace roles={["pilot_administrator","incident_commander","release_manager"]} title="Pilot feature controls" intro={`Review independently gated features for ${pilotId}. Defaults remain disabled.`}><p>Enabling a flag fails while any dependency gate is open. Emergency disabling is allowed and audited.</p><Link href="/admin/feature-flags">Open the global default-deny register</Link></SecureOperationalWorkspace>}
