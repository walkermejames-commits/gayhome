import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{pilotId:string}>}){const{pilotId}=await params;return <SecureOperationalWorkspace roles={["pilot_administrator","commissioner_viewer"]} title="Pilot outcomes" intro={`Optional, aggregate indicators for ${pilotId}. No outcome is used to rank or pressure participants.`}><p className="callout">Groups smaller than five are suppressed. Names, narratives, addresses and sensitive identity attributes are not report dimensions.</p></SecureOperationalWorkspace>}
