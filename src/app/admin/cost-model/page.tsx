import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default function Page(){return <SecureOperationalWorkspace roles={["pilot_administrator","commissioner_viewer","release_manager"]} title="Operating cost model" intro="Editable scenarios with dated sources and confidence. Market prices are inputs, not permanent constants."><p>Annual totals normalize one-off, monthly, quarterly and annual items while preserving quantity, unit cost, source date and notes.</p></SecureOperationalWorkspace>}
