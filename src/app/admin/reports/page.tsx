import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default function Page(){return <SecureOperationalWorkspace roles={["commissioner_viewer","pilot_administrator"]} title="Commissioner reports" intro="Aggregate, minimum-threshold reporting that distinguishes actuals, targets, assumptions, projections and unmeasured outcomes."><p className="callout">Counts below five are suppressed. Reports contain no names, case narratives, exact addresses or sensitive identity and health attributes.</p><p>CSV and accessible PDF rendering use the same privacy-reviewed aggregate report record. External publication remains gated.</p></SecureOperationalWorkspace>}
