import { IncidentForm } from "@/components/operational-forms";import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default function Page(){return <SecureOperationalWorkspace roles={["incident_commander","safeguarding_lead","pilot_administrator"]} title="Record an incident" intro="Use the versioned incident API for structured creation. It records severity and impact without case narratives."><div className="callout">Never paste evidence contents, abuse detail or an affected person&apos;s story here. Reference a protected record instead.</div><IncidentForm/></SecureOperationalWorkspace>}
