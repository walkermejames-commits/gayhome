import { SecureOperationalWorkspace } from "@/components/secure-operational-workspace";export default async function Page({params}:{params:Promise<{incidentId:string}>}){const{incidentId}=await params;return <SecureOperationalWorkspace roles={["incident_commander","safeguarding_lead","pilot_administrator"]} title="Incident containment" intro={`Controlled actions and review for incident ${incidentId}.`}><ul><li>Disable a feature or registration</li><li>Disable evidence, email or advocate invitations</li><li>Hide a dangerous service reference</li><li>Pause a pilot or prepare rollback</li></ul><p>Every action records actor, reason, target, previous state and new state.</p></SecureOperationalWorkspace>}
