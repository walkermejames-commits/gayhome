import { getCatalog } from "@/server/catalog"; export default async function Page(){const c=await getCatalog();return <><h1>Find your council</h1><p className="lede">Choose the area you are in now. You can still ask for homelessness help if your last settled address was elsewhere or you have no postcode.</p><div className="field"><label htmlFor="postcode">Postcode (optional)</label><input id="postcode" autoComplete="postal-code"/><span className="field-note">Choose a district below if you do not know a postcode.</span></div><ul className="service-list">{c.councils.map(x=><li className="service-card" key={x.id}><h2><a href={`/councils/${x.id}`}>{x.name}</a></h2><p>{x.area}</p><p className="meta">Verified {x.verifiedOn}</p></li>)}</ul></>}
