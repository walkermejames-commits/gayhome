/* eslint-disable @next/next/no-html-link-for-pages */
import { notFound } from "next/navigation";import { getCatalog } from "@/server/catalog";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,c=await getCatalog(),x=c.councils.find(v=>v.id===id),source=x&&c.sources.find(s=>s.source_id===x.sourceId);if(!x)notFound();return <><h1>{x.name}</h1><p className="lede">This is likely the council route for {x.area}. Local connection is not required before you can ask to make an application.</p><section className="service-card"><h2>Contact the housing team</h2><p><strong>Telephone:</strong> {x.phone}</p><p><a href={x.homelessnessUrl}>Open the official online route</a></p><p>{x.hoursNotes}</p><p>{x.routeNotes}</p></section><section><h2>What to say and record</h2><p>Say that you want to make a homelessness application, explain where you can safely stay tonight, and ask for reasonable adjustments if needed.</p><p>Record the date, time, team, what you were told and any reference number. If refused, ask for the decision and reasons in writing.</p><p><a href="/letters">Prepare a written request</a></p></section><p className="meta">Verified {x.verifiedOn}. Source: {source?<a href={source.source_url}>{source.source_name}</a>:"controlled council record"}.</p></>}
