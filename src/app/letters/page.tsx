import{getCatalog}from"@/server/catalog";export default async function Page(){const c=await getCatalog();return <><h1>Letters and scripts</h1><p className="lede">Prepare what to say, remove sensitive details and review every placeholder.</p><ul className="service-list">{c.scripts.map(s=><li className="service-card" key={s.script_id}><h2><a href={`/letters/${s.script_id}`}>{s.title}</a></h2><p>{s.audience}</p><p>{s.use_note}</p></li>)}</ul></>}
