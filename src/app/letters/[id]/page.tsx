import{notFound}from"next/navigation";import{getCatalog}from"@/server/catalog";import{EditableScript}from"@/components/editable-script";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,c=await getCatalog(),s=c.scripts.find(x=>x.script_id===id);if(!s)notFound();return <><h1>{s.title}</h1><p className="lede">{s.use_note}</p><EditableScript text={s.script_text}/></>}
