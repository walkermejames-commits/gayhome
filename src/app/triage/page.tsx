import { TriageForm } from "@/components/triage-form"; import { getCatalog } from "@/server/catalog"; export const metadata={title:"Get help now"}; export default async function Page(){const c=await getCatalog();return <><h1>Get help now</h1><p className="lede">Answer only what is needed to find a safe route. This is not an application and nothing is sent.</p><TriageForm areas={[...new Set(c.councils.map(x=>x.area))]}/></>}
