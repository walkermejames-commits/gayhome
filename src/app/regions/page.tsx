import Link from "next/link";import { regionRegistry } from "@/phase6/regions";
export default function Page(){const regions=regionRegistry.filter((region)=>region.status==="published");return <><h1>Areas currently available</h1><p className="lede">Only regions with a controlled published dataset appear here.</p><ul className="service-list">{regions.map((region)=><li className="service-card" key={region.id}><h2><Link href={`/regions/${region.id}`}>{region.name}</Link></h2><p>Dataset {region.datasetRevision}</p></li>)}</ul><p className="callout">Sussex is in data preparation and is not published or used for routing.</p></>}
