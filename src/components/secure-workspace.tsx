import Link from "next/link";import { currentSession } from "@/server/auth-cookie";import { getPool } from "@/server/db/pool";
export async function SecureWorkspace({title,intro,requiredRole,children}:{title:string;intro:string;requiredRole?:"administrator"|"safeguarding_reviewer";children?:React.ReactNode}){const session=await currentSession();if(!session)return <><h1>Sign in required</h1><p>This workspace contains private case information and needs a valid session.</p><Link href="/profile">Go to My profile</Link></>;if(requiredRole){const role=await getPool().query("SELECT 1 FROM professional_roles WHERE user_id=$1 AND role=$2 AND status='active' AND (expires_at IS NULL OR expires_at>now())",[session.userId,requiredRole]);if(!role.rowCount)return <><h1>Access denied</h1><p>You do not have an active, verified role for this review queue. Hiding a link is never used as the access control.</p></>}return <><h1>{title}</h1><p className="lede">{intro}</p>{children}</>}
