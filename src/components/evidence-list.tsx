"use client";import{useState}from"react";export function EvidenceList({items}:{items:{id:string;area:string;description:string;safety:string}[]}){const[state,setState]=useState<Record<string,string>>({});return <ul className="service-list">{items.map(x=><li className="service-card" key={x.id}><h2>{x.area}</h2><p>{x.description}</p><p className="callout">{x.safety}</p><label>Status <select value={state[x.id]??""} onChange={e=>setState(v=>({...v,[x.id]:e.target.value}))}><option value="">Not marked</option><option>Available</option><option>Requested</option><option>Unavailable</option><option>Preparing</option></select></label></li>)}</ul>}
