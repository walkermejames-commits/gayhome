"use client";
import { useState } from "react";
const initial=[{phase:"Do this now",title:"Check where you can be safe tonight",reason:"Your immediate safety comes first."},{phase:"Do this today",title:"Contact the council housing team",reason:"Ask to make a homelessness application and record the response."},{phase:"If refused",title:"Ask for the decision in writing",reason:"A written decision helps you understand and challenge what happened."}];
export function ActionPlan(){const[done,setDone]=useState<string[]>([]);return <div>{initial.map(item=><section className="service-card" key={item.title}><p className="status">{item.phase}</p><h2>{item.title}</h2><p>{item.reason}</p><label><input type="checkbox" checked={done.includes(item.title)} onChange={e=>setDone(current=>e.target.checked?[...current,item.title]:current.filter(value=>value!==item.title))}/> Mark complete</label><details><summary>Add a private session note</summary><textarea aria-label={`Note for ${item.title}`}/><p className="field-note">This note is not saved after this browser session.</p></details></section>)}</div>}
