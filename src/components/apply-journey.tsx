"use client";
import { useState } from "react";
export function ApplyJourney({title}:{title:string}){const[review,setReview]=useState(false);return <div className="form-stack"><p className="callout">Nothing is sent automatically. Complete only what you feel safe sharing, then review or remove every detail.</p>{["Your name","Where you are staying now","A safe way to contact you","What help you need"].map(label=><div className="field" key={label}><label>{label}<textarea/></label><span className="field-note">You can skip this, use it once, or remove it before export.</span></div>)}<section className="review-panel"><h2>Review your {title.toLowerCase()}</h2><label><input type="checkbox" checked={review} onChange={e=>setReview(e.target.checked)}/> I have reviewed the details and removed anything I do not want to share.</label></section><div className="button-row"><button className="primary" type="button" disabled={!review} onClick={()=>window.print()}>Print or save as PDF</button><button className="secondary" type="button" disabled={!review}>Copy reviewed text</button></div><p aria-live="polite">{review?"Ready for you to copy, print or take to the official route.":"Review is required before export."}</p></div>}
