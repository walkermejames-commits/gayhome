"use client";import { useState } from "react";
export function AdvocateInviteForm(){const [message,setMessage]=useState("");async function submit(form:FormData){const permissions=form.getAll("permissions").map(String);const response=await fetch("/api/v1/advocate/invitations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({caseId:form.get("caseId"),email:form.get("email"),helperType:form.get("helperType"),expiresAt:new Date(String(form.get("expiresAt"))).toISOString(),permissions,sensitiveCategories:[]})});const result=await response.json();setMessage(response.ok?`Invitation prepared. ${result.warning}${result.data.inviteToken?` Development token: ${result.data.inviteToken}`:""}`:(result.error?.message??"Could not prepare invitation."))}return <form className="form-stack" action={submit}><label className="field">Case ID<input name="caseId" required/></label><label className="field">Their email<input name="email" type="email" autoComplete="email" required/></label><label className="field">How they help<select name="helperType"><option>Friend</option><option>Family member</option><option>Support worker</option><option>Solicitor</option><option>Housing adviser</option><option>Healthcare worker</option><option>Other trusted person</option></select></label><fieldset><legend>What can they do?</legend><label><input type="checkbox" name="permissions" value="view_summary" defaultChecked/> View case summary</label><br/><label><input type="checkbox" name="permissions" value="view_timeline"/> View selected timeline</label><br/><label><input type="checkbox" name="permissions" value="view_deadlines"/> View deadlines</label><br/><label><input type="checkbox" name="permissions" value="add_notes"/> Add notes</label></fieldset><label className="field">Access expires<input name="expiresAt" type="datetime-local" required/></label><button className="primary" type="submit">Review and create invitation</button><p role="status" aria-live="polite">{message}</p></form>}
