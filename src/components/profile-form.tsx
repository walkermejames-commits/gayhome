"use client";import{useState}from"react";export function ProfileForm({section="About me"}:{section?:string}){const[notice,setNotice]=useState("");return <div className="form-stack"><p className="callout">Guest mode: these answers remain only in this page and disappear when you leave. Encrypted saving requires an account.</p><div className="field"><label>{section}<textarea/></label><span className="field-note">Why we ask: to avoid repeating this information in reviewed requests. You can skip, prefer not to say, use once, restrict sharing or delete it.</span></div><fieldset><legend>How may this be used?</legend><label><input type="radio" name="save" defaultChecked/> Use once</label><label><input type="radio" name="save"/> Save on my private account</label><label><input type="checkbox"/> Exclude from automatic reuse</label><label><input type="checkbox"/> Restrict sharing</label></fieldset><button className="primary" type="button" onClick={()=>setNotice("Kept for this page only. Nothing was sent.")}>Keep for this session</button><p aria-live="polite">{notice}</p></div>}
