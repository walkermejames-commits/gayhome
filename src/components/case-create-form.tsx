"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function CaseCreateForm(){const router=useRouter(),[message,setMessage]=useState("");async function submit(formData:FormData){setMessage("Saving…");const response=await fetch("/api/v1/cases",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:formData.get("title"),caseType:formData.get("caseType"),urgency:formData.get("urgency"),currentArea:formData.get("currentArea")||undefined,safeContact:formData.get("safeContact")==="on",discreetMode:formData.get("discreetMode")==="on"})});const result=await response.json();if(response.ok)router.push(`/case/${result.data.id}`);else setMessage(result.error?.message??"The case could not be saved.")};return <form className="form-stack" action={submit}><label className="field">Case title<input name="title" required maxLength={160}/><span className="field-note">Use a neutral title if someone else may see your screen.</span></label><label className="field">What is this about?<select name="caseType" defaultValue="homelessness_application"><option value="homelessness_application">Homelessness application</option><option value="eviction_prevention">Eviction prevention</option><option value="domestic_abuse_relocation">Safe relocation</option><option value="temporary_accommodation_suitability">Accommodation suitability</option><option value="housing_register_dispute">Housing register</option><option value="prison_release">Prison release plan</option><option value="refugee_asylum_accommodation">Refugee or asylum accommodation</option><option value="landlord_harassment">Landlord harassment</option><option value="unlawful_eviction">Unlawful eviction</option><option value="other">Something else</option></select></label><label className="field">Urgency<select name="urgency" defaultValue="standard"><option value="standard">Standard</option><option value="soon">Needs attention soon</option><option value="urgent">Urgent</option><option value="immediate">Immediate risk</option></select></label><label className="field">Current area<input name="currentArea" maxLength={160}/></label><label><input type="checkbox" name="safeContact" defaultChecked/> It is safe to use my chosen contact details</label><label><input type="checkbox" name="discreetMode"/> Use discreet wording for this case</label><button className="primary" type="submit">Create case</button><p role="status" aria-live="polite">{message}</p></form>}
