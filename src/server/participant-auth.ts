import "server-only";import type { NextRequest } from "next/server";import { requireMutation } from "@/server/case-api";import { getPool } from "@/server/db/pool";import { apiError } from "@/server/http";
export async function requireParticipantOwner(request:NextRequest,participantId:string){const session=await requireMutation(request);if(session instanceof Response)return session;const owned=await getPool().query("SELECT 1 FROM pilot_participants WHERE id=$1 AND user_id=$2 AND status NOT IN ('deleted')",[participantId,session.userId]);return owned.rowCount?session:apiError(403,"PARTICIPANT_SCOPE_REQUIRED","This pilot participation record is not linked to your account.");}
