import { describe,expect,it } from "vitest";import { annualisedCost,featureFlagIds,pilotActivationBlockers,suppressSmallCells } from "@/domain/pilot-operations";
describe("Phase 5 fail-closed controls",()=>{it("defines every required flag as a controlled key",()=>{expect(featureFlagIds).toHaveLength(13);expect(featureFlagIds).toContain("production.public_access");});it("blocks activation for status, owners, cohorts, recovery, gates and unsafe defaults",()=>{const blockers=pilotActivationBlockers({status:"draft",maximumUsers:0,owners:{safeguarding:null,support:null},cohortApproved:false,supportConfigured:false,rollbackVerified:false,backupVerified:false},[{id:"SAFEGUARD-001",status:"open",humanApprovalRequired:true}],[{id:"pilot.registration",defaultState:true}]);expect(blockers).toEqual(expect.arrayContaining(["Pilot status is not approved or scheduled.","safeguarding owner is not assigned.","No approved cohort exists.","SAFEGUARD-001 is open.","pilot.registration does not default to disabled."]));});it("suppresses cells below five",()=>{expect(suppressSmallCells([{label:"test",count:4}])).toEqual([{label:"test",count:null,suppressed:true}]);expect(suppressSmallCells([{label:"test",count:5}])[0]).toMatchObject({count:5,suppressed:false});});it("annualises editable cost inputs",()=>{expect(annualisedCost(2,10,"monthly")).toBe(240);expect(annualisedCost(3,25,"quarterly")).toBe(300);});});
