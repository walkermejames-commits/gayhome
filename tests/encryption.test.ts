import { beforeEach,describe,expect,it } from "vitest";import { decryptSensitive,encryptSensitive } from "@/server/security/encryption";import { resetEnvironmentForTests } from "@/server/env";
describe("authenticated encryption",()=>{beforeEach(()=>{process.env.ENCRYPTION_KEY_ID="test-key";process.env.ENCRYPTION_MASTER_KEY=Buffer.alloc(32,7).toString("base64");resetEnvironmentForTests()});it("round trips only in its bound context",()=>{const encrypted=encryptSensitive("sensitive value","profile:one");expect(encrypted).not.toContain("sensitive value");expect(decryptSensitive(encrypted,"profile:one")).toBe("sensitive value");expect(()=>decryptSensitive(encrypted,"profile:two")).toThrow()});it("detects tampering",()=>{const encrypted=encryptSensitive("value","field");expect(()=>decryptSensitive(`${encrypted}x`,"field")).toThrow()})});
